"""Local (SDK) derivation strategy identifier -- distinct from `model.enums.DerivationScheme` (wire format), same pattern as the backend's Domain/Contracts mirroring."""

from __future__ import annotations

TRON_BIP44_HARDENED_ACCOUNT = "TRON_BIP44_HARDENED_ACCOUNT"
